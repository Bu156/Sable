---
default: patch
---

Fix AltStore/SideStore iOS sources: the source manifests (`altstore-source.json` for stable, `altstore-source-nightly.json` for nightly) are now committed at the repository root with their full version history on every release, and can be added via their raw GitHub URLs.
